
/* spellchecker: disable */

// import { should } from 'chai';
// import { useFakeXMLHttpRequest, SinonFakeXMLHttpRequest, SinonFakeXMLHttpRequestStatic } from 'sinon';
// // import { Color } from '../source/color';
// import { ColorScale } from '../source/colorscale';

// import testscalesData from './testscales.json';

// import { Color, Space } from '../source/color';
// import { ArrayType, ColorScale, InterpolationHint } from '../source/colorscale';

/* spellchecker: enable */

// should();

describe('ColorScale Fetch', () => {

    // let xhrFake: SinonFakeXMLHttpRequestStatic;
    // let requests: Array<SinonFakeXMLHttpRequest> = new Array<SinonFakeXMLHttpRequest>();

    // beforeEach(function () {
    //     xhrFake = useFakeXMLHttpRequest();

    //     requests.length = 0;

    //     xhrFake.onCreate = function (xhr: SinonFakeXMLHttpRequest) {
    //         requests.push(xhr);
    //     }.bind(this);
    // });

    // afterEach(function () {
    //     xhrFake.restore();
    // });

    /**
     * @todo: implement test! Perhaps find replacement for XML
     */
    // it('should be loadable from preset', async () => {

    //     const testScalesJson = JSON.stringify(testscalesData);
    //     await ColorScale.fromPreset('./testscales.json', 'magma', 7);

    //     requests[0].respond(200, { 'Content-Type': 'text/json' }, testScalesJson);
    // });

});
